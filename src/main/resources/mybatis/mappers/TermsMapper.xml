<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.kr.exitobiz.Mappers.Cms.TermsMapper">
    <select id="getTermService" resultType="String">
        select content from itda_web.terms where index = 1
    </select>

    <update id="saveTermService" parameterType="hashMap">
        update itda_web.terms set
            content = #{content}
           , updt_dt = LOCALTIMESTAMP(0)
        where index = 1
    </update>

    <select id="getTermPrivacy" resultType="String">
        select content from itda_web.terms where index = 2
    </select>

    <update id="saveTermPrivacy" parameterType="hashMap">
        update itda_web.terms set
            content = #{content}
           , updt_dt = LOCALTIMESTAMP(0)
        where index = 2
    </update>

    <select id="getTermLocate" resultType="String">
        select content from itda_web.terms where index = 3
    </select>

    <update id="saveTermLocate" parameterType="hashMap">
        update itda_web.terms set
            content = #{content}
            , updt_dt = LOCALTIMESTAMP(0)
        where index = 3
    </update>

    <select id="getPersonMobile" resultType="String">
        select content from itda_web.terms where index = 4
    </select>
</mapper>